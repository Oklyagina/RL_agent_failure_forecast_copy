# Minimal verified requirements for the ENN uncertainty-quantification module
# IMPORTANT: requires Python 3.9 or 3.10
#   (tensorflow==2.12.1, ray==2.5.1 and torch==2.1.2 have no wheels for >=3.11)
# Grid2Op is pinned to 1.9.8 = the version used by the InteractiveAI simulator,
# so the action format stays consistent. Do not upgrade silently.

setuptools==75.8.0
numpy==1.24.3
pandas==2.1.4
scikit-learn==1.7.2
matplotlib==3.8.4
joblib==1.5.1
requests==2.32.4
urllib3==2.5.0
tqdm==4.66.6
shap==0.44.1
torch==2.1.2
torchmetrics==1.5.2
tensorflow==2.12.1
optuna==3.5.1
nni==2.10.1
ray[rllib]==2.5.1
lightning==2.5.2
Grid2Op==1.9.8
Gymnasium==0.26.3
l2rpn-baselines==0.8.0
LightSim2Grid==0.10.3
defopt==7.0.0
ubelt==1.3.7
